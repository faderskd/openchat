import forms from 'openchat/utils/forms';
import { module, test } from 'qunit';

module('Unit | Utility | forms');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = forms();
  assert.ok(result);
});
